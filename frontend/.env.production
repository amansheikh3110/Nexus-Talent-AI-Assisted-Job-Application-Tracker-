# On Vercel, the frontend and backend share the same domain.
# The /api/* rewrites in vercel.json forward calls to the backend serverless function.
# So no absolute URL is needed — relative /api paths work automatically.
VITE_API_URL=
